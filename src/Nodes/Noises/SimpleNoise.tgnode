metadata
    name "Simple Noise"
    color 0x3483EBFF
    category "Core/Noise"

    varname Out "Out"
    display Out
metadata

output greytex Out

exec
{
    for x=1, sizeX do
        for y=1, sizeY do
            Out[x][y] = math.random(0,255)
        end
    end
}